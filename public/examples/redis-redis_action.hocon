actions.redis.my_webhook_1 {
    local_topic = "sample_topic"
    parameters = {
        #substruct(bridge_redis:action_parameters)
    }
    connector = "my_connector"
    resource_opts = {
        #substruct(redis:action_resource_opts)
    }
}