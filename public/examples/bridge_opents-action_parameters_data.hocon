actions.opents.my_webhook_1.parameters.data = [
  {
    timestamp = "${timestamp_var}"
    metric = "${metric_var}"
    tags = ${tags_var}
    # or 
    tags = "${tags_template}"
    value = 100
    # or 
    value = 100.0
    # or 
    value = "${value_template}"
  }
]