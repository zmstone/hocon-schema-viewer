gateway.stomp {
  frame = {
    #substruct(gateway:stomp_frame)
  }
  mountpoint = ""
  listeners = {
    #substruct(gateway:tcp_listeners)
  }
  enable_stats = true
  idle_timeout = "30s"
  clientinfo_override = {
    #substruct(gateway:clientinfo_override)
  }
}